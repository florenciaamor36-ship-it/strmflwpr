name: Deploy SaaS Full
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Write index.html
        run: |
          cat << 'EOF' > index.html
          (CONTENIDO COMPLETO AQUÍ)
          EOF
      - name: Write admin-panel.html
        run: |
          cat << 'EOF' > admin-panel.html
          (CONTENIDO COMPLETO AQUÍ)
          EOF
      - name: Commit
        run: |
          git config --local user.email "florenciaamor36@gmail.com"
          git config --local user.name "Zapia AI"
          git add index.html admin-panel.html
          git commit -m "Full Deploy SaaS"
          git push
