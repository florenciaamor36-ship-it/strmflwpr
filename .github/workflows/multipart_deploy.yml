name: Patch MultiPart
on:
  workflow_dispatch:
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Reconstruct index.html
        run: |
          cat part_aa part_ab part_ac part_ad part_ae part_af part_ag part_ah part_ai > index.html
          git config --local user.email "florenciaamor36@gmail.com"
          git config --local user.name "Zapia AI"
          git add index.html
          git commit -m "MultiPart Full Deploy"
          git push
