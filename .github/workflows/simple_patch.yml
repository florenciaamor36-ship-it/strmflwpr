name: Simple Patch
on:
  workflow_dispatch:
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Patch
        run: |
          # Admin Badge
          sed -i 's/id="perfilPlan">DEMO/id="perfilPlan">ADMIN PRO/g' index.html
          # Prex Alias
          sed -i 's/5492236785329/5492236785329/g' index.html # Just to check
          
          git config --local user.email "florenciaamor36@gmail.com"
          git config --local user.name "Zapia AI"
          git commit -am "Simple UI Fix"
          git push
