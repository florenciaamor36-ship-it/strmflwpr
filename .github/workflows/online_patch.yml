name: Online Python Patcher
on:
  workflow_dispatch:
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Patcher
        run: |
          # El script online_patcher.py ya estará en el repo
          python3 online_patcher.py
          git config --local user.email "florenciaamor36@gmail.com"
          git config --local user.name "Zapia AI"
          git commit -am "Final Premium Patch via Python"
          git push
        continue-on-error: true
